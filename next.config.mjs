/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/ficofi-event-register',
  output: 'export',
  images: {
    domains: ['ficoficlientsazurec96aa.blob.core.windows.net'],
  }
};

export default nextConfig;
